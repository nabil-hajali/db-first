# USED CAR DEALER

## Colums

-id INDEX INT PRIMARY KEY AUTO_INCREMENT
-car_brand (VARCHAR(50), NOT NULL)
-model (VARCHAR(50),NOT NULL)
-year (YEAR ,NOT NULL)
-engine_kilometres (INT,NOT NULL)
-fuel_type (VARCHAR(20)NOT NULL)
-damages (TINYINT ,NOT NULL)
-transmission type (VARCHAR(20)NOT NULL)
-hp (INT,NOT NULL)
-doors (INT,NOT NULL)
-vehicle_displacement (INT,NOT NULL)
-color (VARCHAR(30) NOT NULL)
-price (DECIMAL(7,2) NOT NULL)
-product_description (TEXT)